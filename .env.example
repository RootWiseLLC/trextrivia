# PostgreSQL
POSTGRES_PASSWORD=your_secure_password

# Domain (for CORS)
DOMAIN=trextrivia.com

# JWT Keys (generate with: ssh-keygen -t rsa -b 4096 -m PEM -f jwtRS512.key)
# Then base64 encode them: cat jwtRS512.key | base64
JWT_PRIVATE_KEY=your_base64_encoded_private_key
JWT_PUBLIC_KEY=your_base64_encoded_public_key
